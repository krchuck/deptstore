<h1> Items </h1>
<%= link_to 'Main Page', root_path %>
<%= link_to 'Add Items', new_department_item_path %>
<% @items.each do |item| %>
  <h2><%= item.name %></h2>
<%= link_to 'Edit Items', edit_department_item_path %>
  

  <% end %>